<%= link_to 'НОВЫЙ ПОСТ', new_post_path, class: 'btn btn-large' %>

<h1>
  <% if @category %>
      <%= 'Список публикаций категории '+@category.name %>
  <% else %>
      Список публикаций
  <% end %>
</h1>

  <%= render @posts %>

  <br>

