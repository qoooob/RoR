<h2> <%= @post.title %></h2>

<h5>
  <strong>Категории:</strong>
  <%= render @post.categories %>
</h5>

<% if @post.user.present? %>
    <h5>
    <strong>Автор:</strong>
    <%= @post.user.email %>
    </h5>
<% end %>

<p>
  <%= @post.body %>
</p>



<% unless @post.comments.empty? %>
    <strong><%= 'Комментарии ('+@post.comments.count.to_s+')'%></strong>
    <% @post.comments.each do |comment| %>
            <p><%= comment.body %></p>
        <% if comment.user.present? %>
            <p>
              <strong>Автор:</strong>
              <%= comment.user.email %>
            </p>
        <% end %>
        <% if comment.user == current_user %>
            <p><%= link_to "Удалить", comment, method: :delete, data: {confirm: 'Точно удаляем?'} %></p>
            <p><%= link_to "Изменить", edit_comment_path(comment) %></p>
        <% end %>
    <% end %>
    <strong>Новый комментарий:</strong>
<% else %>
    <strong>Комментариев нет. Оставьте комментарий первым.</strong>
    <br>
<% end %>

<%= render 'comments/form' %>

<% if @post.user == current_user %>
    <%= link_to 'Изменить', edit_post_path(@post) %> |
    <%= link_to 'Удалить', @post, method: :delete %> |
<% end %>


<%= link_to 'Назад', posts_path %>
